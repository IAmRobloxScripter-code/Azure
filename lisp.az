(extern write fn)

(fn main int ()
    (let str *char "gem\n")

    (let index int 0)

    (loop
        (is (>= index 10)
            (break)
        )
        (() write 1 str 4)
        (= index (+ index 1))
    )

    (loop
        (is (>= index 20)
            (break)
        )
        (() write 1 str 4)
        (= index (+ index 1))
    )
)